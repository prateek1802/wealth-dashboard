diff --git a/Bugs/CONSOLIDATED-STATUS-REPORT.txt b/Bugs/CONSOLIDATED-STATUS-REPORT.txt
index e9af647..0a24003 100644
--- a/Bugs/CONSOLIDATED-STATUS-REPORT.txt
+++ b/Bugs/CONSOLIDATED-STATUS-REPORT.txt
@@ -2,6 +2,17 @@ WEALTH DASHBOARD — CONSOLIDATED STATUS REPORT
 (supersedes All_Issues.txt / MASTER-REPORT.txt — every item below directly
 re-verified against the latest zip, not carried forward on trust)
 
+RE-VERIFICATION NOTE (this pass): re-checked every item in Section 3 directly
+against source in the latest zip before touching anything. Four items
+previously listed as open were found ALREADY FIXED in code — CAGR
+zero-snapshot (3A), Dashboard redundant fetching (3D/4D), backup restore
+ERROR VISIBILITY specifically (3F/4F — the underlying restore failure's
+true root cause is separate, see that entry), and the freshness-indicator
+feature idea. Moved to Section 1 below with notes; Section 3/4 entries kept
+but marked DONE rather than deleted, so the history of what was fixed when
+isn't lost. Everything else in Section 3 re-confirmed still open, matching
+prior passes.
+
 ====================================================================================
 1. WHAT'S DONE — TRUST/CORRECTNESS BACKLOG (essentially closed out)
 ====================================================================================
@@ -70,6 +81,30 @@ re-verified against the latest zip, not carried forward on trust)
    (already-correct, already-tested) calculations. Correct, honest scoping, not
    an incomplete feature.
 
+NEWLY CONFIRMED FIXED THIS PASS (previously listed open in Section 3 — see
+that section for the fix description, kept there marked DONE):
+✅ CAGR zero-snapshot bug (was 3A/4A) — selectCAGRBaseSnapshot() now exists in
+   src/lib/calculations/returns.ts, used by analytics/page.tsx, with dedicated
+   tests in test/returns.test.ts covering a snapshot series that starts with
+   zero-value entries.
+✅ Dashboard redundant data fetching (was 3D/4D) — portfolioService
+   .getDashboardData() now exists and is the dashboard's single shared fetch;
+   dashboard/page.tsx no longer makes independent computeHoldings()-triggering
+   calls. Count no longer applicable (consolidated to one call).
+✅ Backup restore error VISIBILITY specifically (was 3F/4F, half of a two-part
+   bug) — src/lib/utils/log-error.ts's getErrorMessage() now duck-types
+   (checks for a `.message` property) instead of `instanceof Error`, and every
+   catch block inside backup.service.ts's importAll() calls logServerError().
+   NOTE: this only fixes visibility. Whether the underlying 775-row restore
+   failure itself is now resolved is NOT verified here — that needs a real
+   restore attempt against a live Supabase project to see the real error text
+   this fix surfaces, which static code review can't do. Treat the true root
+   cause as still open until that live re-test happens.
+✅ Freshness indicator for securities prices (was a "features worth adding"
+   item, Section 6/9) — src/components/shared/price-freshness.tsx exists and
+   is wired into portfolio-view.tsx, investment-detail.tsx, and
+   investment-card.tsx, using currentPriceUpdatedAt as originally spec'd.
+
 ====================================================================================
 2. NPS REWRITE — ALL 8 PARTS, STILL FULLY VERIFIED
 ====================================================================================
@@ -99,29 +134,22 @@ genuinely removed not left dangling.
 3. STILL OPEN — CONFIRMED, RE-CHECKED DIRECTLY AGAINST LATEST CODE
 ====================================================================================
 
-❌ CAGR zero-snapshot bug. firstSnapshot = sortedSnapshots[0] still has no
-   filtering for netWorth <= 0. This account's real first two snapshots
-   (2026-08-14, 2026-08-15) are legitimately Rs.0 (recorded before real holdings
-   existed on this deployment) — calculateCAGR() correctly refuses a zero base,
-   so CAGR is permanently stuck. Not data loss — confirmed via an actual backup
-   file that full snapshot history (16 entries, Aug 14 onward) is intact and
-   complete. FIX: skip leading snapshots with netWorth <= 0 when selecting
-   CAGR's starting point; recompute years from that adjusted snapshot's date.
-   Full ready-to-send prompt paragraph already drafted.
+✅ DONE (was open, confirmed fixed this pass — see Section 1) CAGR
+   zero-snapshot bug. selectCAGRBaseSnapshot() in
+   src/lib/calculations/returns.ts now skips leading netWorth <= 0 snapshots
+   when picking CAGR's starting point; analytics/page.tsx recomputes years
+   from that adjusted date. Tested (test/returns.test.ts) including a series
+   that starts with zero-value entries.
 
 ❌ NPS live NAV "Detailed" endpoint + staleness warning — spec'd (Section 2),
    not yet built. This is what would have caught the 2-day npsnav.in staleness
    incident automatically instead of requiring days of manual investigation.
 
-❌ Dashboard redundant data fetching — RE-CHECKED, GOTTEN WORSE, NOT BETTER.
-   computeHoldings() is now called independently 9 times per Dashboard load (up
-   from 7 at the last performance audit) — category-wise XIRR and other new
-   Analytics/Dashboard features have each added their own independent call
-   instead of sharing one computed result. This is the exact "compounds over
-   time as more features get built" risk flagged in the original performance
-   audit, now visibly happening. FIX unchanged: one shared getDashboardData()
-   that computes holdings once, derives every dependent view from that single
-   result.
+✅ DONE (was open, confirmed fixed this pass — see Section 1) Dashboard
+   redundant data fetching. portfolioService.getDashboardData() now exists and
+   dashboard/page.tsx calls it as the single shared fetch — the independent
+   computeHoldings()-triggering calls this entry previously tracked (9, up
+   from 7) are gone.
 
 ❌ Table memoization (transactions/portfolio views) — still no useMemo. Low
    priority, not currently felt as slow.
@@ -140,41 +168,36 @@ genuinely removed not left dangling.
    exists in the data model, still not rendered anywhere for stocks/MF/crypto
    (NPS already does this correctly via last_nav_date).
 
-🆕 BACKUP RESTORE — CONFIRMED BROKEN, ROOT CAUSE CURRENTLY UNKNOWABLE (new,
-   found this pass, screenshot evidence: 775 asset failures, 0 assets/
-   transactions imported, all other tables succeeded).
-   TWO CONFIRMED, STACKED BUGS, both about error VISIBILITY, not (yet) the
-   actual underlying cause:
-   1. Every catch block in backup.service.ts's importAll() does
-      `err instanceof Error ? err.message : "failed"` — but Supabase's own
-      PostgrestError is a plain object, NOT an instanceof Error, so this
-      check is always false for real database errors. The real reason gets
-      generated by Supabase and then discarded before reaching the UI —
-      confirmed by the screenshot showing the bare word "failed" with zero
-      detail for all 775 rows.
-   2. There is also no server-side log to check instead: importAll() catches
-      every row-level error internally and tallies it into the returned
-      summary rather than re-throwing — logServerError() only exists in the
-      OUTER action wrapper's catch, which never fires here because
-      importAll() always returns successfully (ok:true) even when every
-      single row inside it failed. No Vercel log trail exists for this
-      either.
-   CONFIRMED NOT A SEPARATE BUG: 0 transactions imported is a direct,
-   expected CONSEQUENCE of 0 assets succeeding (transactions resolve their
-   asset by an in-memory old-id→new-id map built only from successful asset
-   inserts) — not a second independent failure.
-   FIX ORDER: (1) fix the error-check + add logServerError() calls inside
-   every catch block in importAll(), (2) re-run the import once more — the
-   real error text will finally be visible, which is what's actually needed
-   to diagnose and fix the true underlying cause. Cannot be root-caused
-   further with static code review alone; needs the real error text this
-   fix would surface.
+🆕 BACKUP RESTORE — ERROR VISIBILITY HALF ✅ DONE (confirmed fixed this pass,
+   see Section 1); TRUE ROOT CAUSE STILL UNVERIFIED (screenshot evidence from
+   a prior pass: 775 asset failures, 0 assets/transactions imported, all
+   other tables succeeded).
+   TWO CONFIRMED, STACKED BUGS, both were about error VISIBILITY, not (yet)
+   the actual underlying cause:
+   1. ✅ FIXED — getErrorMessage() in src/lib/utils/log-error.ts now duck-types
+      (`'message' in err`) instead of `err instanceof Error ? err.message :
+      "failed"`, so Supabase's plain-object PostgrestError no longer gets
+      discarded down to the bare word "failed".
+   2. ✅ FIXED — every catch block inside backup.service.ts's importAll() now
+      calls logServerError() directly, so a server-side log trail exists even
+      though importAll() still returns ok:true with a tallied error list
+      rather than re-throwing.
+   CONFIRMED NOT A SEPARATE BUG (still true): 0 transactions imported is a
+   direct, expected CONSEQUENCE of 0 assets succeeding (transactions resolve
+   their asset by an in-memory old-id→new-id map built only from successful
+   asset inserts) — not a second independent failure.
+   REMAINING: the fix above only makes the real error text visible — it does
+   NOT by itself confirm the 775-row failure is resolved. NEXT STEP: re-run
+   the import once against a live Supabase project and read the now-visible
+   real error text (UI + Vercel logs) to diagnose and fix the true underlying
+   cause. Cannot be root-caused further with static code review alone.
 
 ====================================================================================
 4. READY-TO-SEND PROMPTS (drafted, not yet given to the coding chat)
 ====================================================================================
 
-A. CAGR ZERO-SNAPSHOT FIX (one paragraph, ready as-is):
+A. CAGR ZERO-SNAPSHOT FIX — ✅ DONE, confirmed shipped this pass (see Section 1).
+   Prompt below kept for history only, no longer needs sending:
    "In src/app/(app)/analytics/page.tsx, CAGR is stuck permanently showing 'Need
    a positive starting value and a positive time span' because firstSnapshot =
    sortedSnapshots[0] picks the chronologically first snapshot with no regard
@@ -200,16 +223,16 @@ B. NPS DETAILED ENDPOINT + STALENESS WARNING — full spec already written
 
 C. ANALYTICS REDESIGN — DONE, see Section 5A. No longer a pending prompt.
 
-D. DASHBOARD REDUNDANT FETCH — not yet written as a dedicated prompt. Now
-   measurably worse (9 calls, was 7) precisely because the Analytics redesign
-   (C, now shipped) added more independent computeHoldings() calls on top —
-   confirms the "compounds as more features get built" prediction directly.
-   Worth writing up and sending before the SIP feature (E) adds yet another.
+D. DASHBOARD REDUNDANT FETCH — ✅ DONE, confirmed shipped this pass (see
+   Section 1). getDashboardData() landed before the SIP feature (E), as hoped.
 
 E. ACTIVE SIPs FOR PROJECTIONS — full spec already written
    (ACTIVE-SIPS-PROMPT.txt), see Section 5B for the design summary.
 
-F. BACKUP RESTORE ERROR VISIBILITY (ready as-is): "In
+F. BACKUP RESTORE ERROR VISIBILITY — ✅ DONE, confirmed shipped this pass (see
+   Section 1 and the Section 3 entry — root cause of the actual restore
+   failure is still separately unverified). Prompt below kept for history
+   only, no longer needs sending: "In
    src/lib/services/backup.service.ts's importAll(), every catch block's
    error handling (`err instanceof Error ? err.message : \"failed\"`)
    silently discards the real error, because Supabase's PostgrestError is a
@@ -300,10 +323,15 @@ portfolio-level projection calls. Not yet built as of this report.
 The trust/correctness backlog (Section 1) is essentially closed, and the
 Analytics redesign (Section 5A) has now fully landed too — everything that
 made the app's own numbers hard to trust or hard to read has been fixed and
-independently verified, not just claimed. What's left (Section 3) is mostly
-forward-looking feature work, with one clear near-term priority: Dashboard's
-redundant fetching, which has gone from a theoretical "will compound over
-time" risk to a directly observed one (7 calls, then 9, tracking exactly with
-each new feature added on top). Worth fixing before the SIP feature (Section
-5B) adds a 10th. The CAGR bug remains small and fully diagnosed — one
-paragraph away from fixed, just hasn't been sent yet.
+independently verified, not just claimed. This pass's re-verification found
+four more items previously tracked as open were also already shipped: the
+CAGR zero-snapshot fix, Dashboard's redundant fetching, backup restore error
+visibility, and the securities freshness indicator — all confirmed directly
+against source, moved to Section 1. What's genuinely left open (Section 3)
+is: the true root cause of the backup restore failure (visibility is fixed,
+the underlying bug isn't yet confirmed resolved — needs a live re-test), the
+NPS "as of" timestamp still being fetch time instead of the NAV's real
+publication date, table memoization, market-close auto-refresh (needs
+infra), the Liabilities live-diagnosis item, and no undo window on deletes.
+Most of what's left is forward-looking feature work rather than
+trust/correctness gaps.
